const index = require('..');
/**
 * File settings
 */
exports._isFileHidden = true;
exports._extension = '';

const files = {
  SunOS: {
    
  }
}

exports.evaluateFile = function (args) {
  console.log(`Help`);
};
